apiVersion: v1
kind: Service
metadata:
  name: hello-world
  labels:
    app: hello-world-service
spec:
  type: NodePort
  selector:
    app: hello-world
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80